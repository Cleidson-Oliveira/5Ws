import styled from "styled-components";

const Wrapper = styled.div`
    display: flex;
    padding: 2.5rem;

    section {
        width: 50%;
        padding: 1rem;

        img {
            width: 100%;
        }
    }
`;

export default Wrapper